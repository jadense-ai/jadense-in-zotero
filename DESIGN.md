# Jadense in Zotero Design Guide

This document defines the current design of the Zotero XPI: the independent Manager, compact item-pane panel, native reader tools, and PDF figure interactions. Its palette, typography, borders, and brand guidance are described below so the plugin can be developed independently.

## Design Direction

Jadense in Zotero is a reading, local Chat, figure-interpretation, literature-analysis, and Jadense-account workbench with one-way upload. Use a white canvas, green-gray side chrome, near-black text, thin borders, restrained green state signals, and compact controls.

The plugin has three contextual surfaces:

| Surface | Role | Scope |
| --- | --- | --- |
| Jadense Manager window | Primary workbench | Local Chat, translation history, independent literature-analysis history/configuration, linked literature/files, Jadense account/points/check-in status, connection settings, upload |
| Zotero item-pane panel | Lightweight contextual panel | Current item/context status, selected-item export, quick manager entry |
| Zotero PDF reader | Reading actions | Ask about current document in a new Chat, interpret an SDT-recognized figure in a new or current Chat, analyze in the independent analysis workspace, translate selection, quote selection |

Collection upload remains discoverable through Tools, the collection context menu, and Manager. Reading actions must not require a target Jadense folder.

## Display Language And Theme

Plugin-owned labels, tooltips, accessible names, empty/loading/error states, guide copy, and Reader controls support Simplified Chinese and English. `跟随 Zotero` maps Chinese host locales to Chinese and other host locales to English. Preserve proper names, model IDs, user literature, source text, AI output, prompts, and saved history; UI language does not select an AI response language or a translation direction.

Use the same light/dark palette on the Manager, the plugin's native Preferences pane, item-pane content, and Reader-owned tools, language popovers, translation panels, and figure controls. Apply theme attributes and variables only to plugin-owned roots. Do not recolor Zotero chrome, the PDF page, native annotations, or surrounding third-party controls. Missing/invalid display preferences fall back locally and cannot block reading, Chat, or uploads. Honor a saved legacy Manager theme until the user chooses a new general theme.

## Independent Manager Window

The Manager opens from `Tools > Jadense`, Configure, native context menus, reader `解析 / 引用`, and the independent figure controls. Reuse the window, not necessarily the conversation: reader `提问` opens the Reader sidebar with a new local conversation draft named after the current paper, and the first question creates the session; `图片解读（开启新对话）` creates and immediately sends a figure-specific conversation; `图片解读（追加在当前对话）` keeps the active conversation, title, history, and sources; reader `解析` opens the independent analysis workspace and never creates a Chat session or message. Translation remains inside the reader and does not open Manager or create a conversation/server session.

### Layout

- Use a 36px shared top bar above a narrow green-gray navigation rail and one docked content panel. The first control is the navigation expand/collapse button, using a rounded rectangle with a left vertical divider (16px outline icon); keep its existing preference and accessible state. Help, a clickable Jadense logo, and GitHub follow it in that order. The logo opens `https://jadense.cn` in the default browser; do not show a Jadense wordmark in the shell. Do not repeat brand or toggle rows inside the rail.
- Open the Manager as a non-dialog chrome window (`dialog=no,titlebar,resizable`). On the verified Windows 11 build 26200 / Zotero 10.0.2 combination, `customtitlebar="true"` integrates the top bar with the native frame; expose Minimize, Maximize/Restore and Close through Gecko window methods. Use native `-moz-window-dragging: drag` on blank chrome and `no-drag` on controls, preserving system double-click and edge resize. macOS, Linux, unknown Windows builds and Zotero versions retain the native titlebar and use the same internal toolbar. Reopening focuses/restores the same Manager and keeps its conversation. This supersedes the former mandatory-native-titlebar rule; do not use `hidechrome` or JavaScript movement to emulate the frame.
- Default new windows to a 1360 × 860 content area, bounded by the available screen work area including native window chrome. Do not resize an existing window when it is reopened.
- Main navigation has `新对话 / 文献解析 / 攻玉学术`; clicking `新对话` opens a fresh in-memory Chat draft, and the first sent message creates the local session. The conversation list only manages existing sessions; each row's `更多` menu offers title editing and deletion. `上手指南` moves to Help and retains `guide`; Settings remains a section with its gear-icon entry in the sidebar footer. Preserve the internal compatibility section value `migrate` for `攻玉学术`, including old upload/context-menu entry URLs.
- Pin the connection indicator, theme toggle, and Settings button to the sidebar bottom. The toggle persists an explicit light/dark choice shared with `设置 → 常规`; selecting `跟随 Zotero` there restores automatic following. Theme and Settings use ghost icons with tooltips and accessible names; the footer becomes vertical when the rail is collapsed. Keep the active Settings state visible.
- Do not add a content-wide status strip or a Chat title/description; brand/help belong in the shared top bar. The workbench starts directly with the conversation layout; account, folder, and upload details belong in `攻玉学术`, not persistent page chrome.
- Titlebar, sidebar and outer spacing share `--jdx-sidebar` (#f7f8f5 light, #1a201c dark). The content panel has an 8px outer inset from the shell (the navigation owns its adjacent spacing), one 8px radius, no decorative border or shadow. `.jdx-chat-workbench` retains its layout/drop/collapse nodes but has no independent radius, border or outer padding. All sections use the same panel; keep cards that group actual content. Dark inner surfaces brighten toward the message/composer field. The composer keeps its interactive hairline but no decorative shadow.
- Non-Chat Manager sections scroll as a whole and reserve a stable scrollbar gutter plus content inset, so the platform scrollbar never overlays the right edge of a section card.
- Use stable widths and fixed control heights so status changes do not shift the layout.
- Prefer dense rows, compact labels, and clear disabled states over explanatory copy.
- At widths of 820px or less, collapse the main navigation by default unless the user has saved a preference. At heights of 740px or less, reduce chrome/input spacing so messages and Send/Stop remain usable at 760 × 620.

### Help And Manual Updates

- Help contains Getting started, About and Check for updates. Its menu supports arrows, Home/End, Escape and normal Tab navigation; transient content is excluded from native dragging.
- About is a compact native HTML dialog containing the logo, plugin name, actual installed AddonManager version, purpose, website and repository. Escape dismisses it and restores focus to Help. The same theme and startup Chinese/English locale apply.
- GitHub always opens `https://github.com/jadense-ai/jadense-in-zotero` through Zotero's default-browser API. Never navigate the privileged Manager to external content.
- Only an explicit Check for updates or Retry fetches GitHub `/repos/jadense-ai/jadense-in-zotero/releases/latest`, without credentials, with a 10-second abort timeout. Only stable non-draft metadata is used; additive fields are ignored. Compare the installed version and normalized tag with `Services.vc.compare`, never lexical sorting. Display checking, current, newer-release, newer-local-version (no downgrade), or failure.
- Display both versions and an Open releases action when available. Rate limits, missing stable releases, invalid data, absent runtime version/comparator and network errors remain in the help dialog with Retry and a fixed repository release-page fallback. Construct tag URLs within the fixed repository instead of trusting response URLs. No automatic download/install, polling, business API, history migration or `update_url` change.
- Native evidence covers Windows 11 build 26200 / Zotero 10.0.2 at 100% display scaling. Isolated Gecko `layout.css.devPixelsPerPx=1.5` additionally covers 150% content/Gecko frame scaling; it does not certify changing Windows system display scaling or multi-monitor DPI transitions. Other OS/versions remain unverified and use native chrome.

### Getting Started Guide

The `上手指南` section (`guide`) is bundled with the XPI. Its own sticky, green-gray chapter sidebar switches between `文献解析 / 图片解读 / 实时翻译 / AI 对话`, with separate automatic-recognition and manual-capture instructions in the image chapter. Use a vertical tablist, Up/Down/Home/End keyboard navigation, one focusable active tab, and one visible reading panel. Switching chapters returns to the top; switching away from the guide preserves the selected chapter until the window closes. Reuse Manager theme tokens and retain the chapter sidebar at the 760 × 620 minimum window size. Reading and chapter navigation require neither a connection nor the Zotero runtime. Keep the content as user-facing instructions with actual UI names, default shortcuts, setup steps, results, and relevant feature limits.

### Chat And Research Tools

- Chat history lives below navigation in the workbench sidebar, scrolls above the footer, collapses with navigation, and stays available across sections. Match Webapp Lucide conversation/new/search/more icons; offer title search, new/select, and a separate more toggle on every conversation row. Deletion targets that row, including inactive conversations, and retains confirmation and generation guards. The sidebar is 260px wide (228px at <=820px). The list scrollport reaches the sidebar right edge; reserve 8px between rows and the scrollbar inside the scrollport. Keep search/header insets independent. The Chat body contains messages/composer and right details. Only details retain a header toggle and saved preference; default collapsed at <=1100px, where expanded details use an overlay.
- Keep `关联条目 / 关联文件` in the detail panel. Both open Zotero's native multi-select library/collection tree, including attachments; do not depend on the main window's current selection. Cancel leaves the conversation unchanged, and confirmation/cancel returns focus to Manager. Dragging native Zotero items anywhere into Chat also adds explicit sources.
- Group sources under the Zotero bibliographic item: `引用信息`, `PDF 文件`/other attachments, and `引用选文` are distinct children. Keep source numbers from the actual request order, even when grouping changes their visual order. A bare `PDF` title must not become a separate anonymous paper. Each paper group can collapse independently; the list owns its full-height scroll region.
- Child titles open the exact local source, × removes only that child, and `移除整组` removes all associations under that paper without deleting Zotero content. `定位 Zotero 条目` opens the verified bibliographic item. No `解析文献` or per-file `解析` action belongs in Chat; launch analysis only from the PDF reader, targeting that reader's attachment.
- Show extraction limits and unreadable files inline. State that sending includes metadata and extracted text, not the original file; locally stored history does not imply offline AI.
- Quote selected text into the composer with citation/page information and keep any existing draft. Translation defaults to English → Simplified Chinese, streams into a movable, resizable non-modal reader panel, and archives source/result objects in a separate bounded profile-local history. The history page uses a two-column source/result layout that stacks on narrow windows. Each history title is an actionable native-source control: revalidate the saved attachment identity, then open that exact attachment in Zotero Reader at the saved physical page. A display `pageLabel` never substitutes for `pageIndex`; a stale or mismatched identity produces an inline failure for that link only and never falls back to title, path, or web-URL navigation.
- Disable session switching/deletion while preparing or generating; keep Stop available. While busy, the detail heading also offers Stop so its compact overlay cannot hide the only cancellation control. A failed annotation write retains the AI result and reports partial counts.
- The open detail panel shows source count and unreadable-file warnings in its summary. Rows show extraction state, character count, and a short quote preview. Removing a source moves keyboard focus to a visible adjacent source, a remaining collapsed group summary, or the association control; never focus a hidden child.
- Render user and AI messages as Markdown, including during streaming: headings, emphasis, nested lists, quotes, fenced/inline code, tables, and web links. Code and tables scroll inside the message without widening the window. Local history and Copy retain the same bounded Markdown source.
- Disable raw HTML execution and keep active Markdown URLs limited to HTTP(S)/mailto. Open links through Zotero's system-browser entry, never navigate the privileged Manager to model content. Markdown images appear as explicit links, without automatic remote/local resource loading; user-provided attachments use the dedicated local image renderer; a formatting failure degrades only the affected message.
- Chat messages use neutral right-aligned user bubbles (up to 85% width) and full-width, borderless AI document text, without a sender-name header. A persistent icon-only action row with localized native tooltips and accessible names follows the body and any image: Copy retains bounded Markdown, plain-text copy uses visible rendered text, and research messages retain Open original PDF. Actions remain keyboard accessible during streaming; an empty message has disabled copy controls.
- The native textarea shares a 16px rounded card with a single compact footer: image attachment and model selection on the left, circular Send/Stop controls on the right. It grows with the draft to 200px and then scrolls internally; keyboard and availability guidance stays below the card. Keep native textarea semantics and the existing IME/send shortcuts; no rich-text composer is introduced.
- Reuse message nodes during streaming. Follow new content only while the user remains near the bottom; offer `回到最新` when reading above it.
- Allow drafting during generation while disabling Send. Show the Ctrl/⌘ + Enter shortcut and respect IME composition. User cancellation is neutral; genuine request/write failures retain error styling. Copy uses the same bounded text shown and saved in the conversation.
- Preserve unsent drafts per session within the open Manager window. A reader-created conversation starts with an empty draft and no unrelated sources/history; returning to the previous conversation restores its draft. Drafts are not persisted across closing the window.
- The Chat model trigger follows the webapp: 32px high, medium-weight 13px text, quiet fill, content-sized up to 256px, with a chevron. Its 304px rounded popup uses searchable grouped 36px rows with local source icons, multipliers, a selection check or subscription lock. Full descriptions remain in native hover tooltips and accessible descriptions; the list grows up to 448px within the viewport. Other selectors keep their existing detailed rows. The plugin does not expose thinking-effort controls because its request contract does not support them.
- Place one compact model selector at the left of the composer footer, combining Jadense intelligent routes, direct models, and configured BYOK models. Show description, supported inputs, consumption multiplier, and minimum subscription where available. Locked choices remain disabled, show the required plan and reason, and keep an upgrade marker visible even when selected and collapsed. Apply this shared presentation to feature settings, analysis configuration, and native Preferences. With a connected Jadense account, unset or legacy account-default selections use `deepseek-v4-flash-vision-exp`; retain existing explicit model/route and BYOK choices, including saved previous-default IDs. Do not show an account-default option. Disable the selector during generation; a catalog display failure must not block requests using the saved selection or plugin default.
- Model/route subscription errors explain that the user can choose an available model in `设置 → 功能配置` or upgrade their subscription. Clarify that buying points does not remove the restriction. Keep token renewal, insufficient points, and BYOK/provider failures distinct; do not automatically change the model or retry the request.
- The new-conversation figure action creates `图片解读：<图注前缀或论文标题>`, automatically associates the verified parent literature plus the current PDF's locally extractable text within the existing 80-page/source budgets, then sends immediately. Extraction failure keeps the available metadata/PDF reference and does not block the image request. The current-conversation action reuses the active session, or a normal empty fallback session when none exists, without changing its title, prior messages, drafts, or sources.
- Each figure action's concise visible user message shows paper, page, `图注` or `未识别到高置信图注`, and `附件：已附图`, followed by its own image preview with click-to-expand. Never expose a base64 URL, hidden prompt, transport shape, or private Reader details in the log.
- The composer offers `上传图片` for one PNG/JPEG per turn, plus paste/drop. Preview, replace, or remove the pending image; allow image-only sends. Image drafts follow their session within the open window. Uploaded images use the AI Chat model; Reader images use the image-interpretation model. Both share local attachment storage and message rendering.
- Persist each sent image in the profile's dedicated attachment directory and keep only its UUID/name/origin in message history. Reopening a conversation restores its images, and follow-ups attach the latest sent image without duplicating previews on text-only turns. A later image replaces the request context, preserving earlier message images. Missing or unsaved attachments show a local notice; readable text remains usable. Old text-only records cannot recover images that were never stored.
- Figure requests use the current Chat model when automatic following is enabled, otherwise the saved image-interpretation model choice; the composer mirrors the effective choice while an image is present, with the existing streaming/error UI. Jadense temporary Chat and the three BYOK protocols project the same image into their native latest-user parts; an unsupported vision model shows the Provider error in place and never switches route. The request-only prompt requires Simplified Chinese Markdown, direct-image/caption/inference separation, no invented unreadable numbers, no claim of reading absent paper text, and untrusted treatment of the paper title, caption, and text inside the image.
- Group the multiline input and its footer in one bordered surface. Keep the hint on the left and Send/Stop inline on the right, below the full-width textarea. Hints may wrap in compact windows. Disable the native resize grip; long drafts scroll inside the bounded textarea. Show focus on the shared border and retain each button's keyboard focus indicator.

### Literature Analysis Section

- `文献解析` retains `解析历史 / 解析配置`. Its paper detail offers an attachment selector and `全文 Markdown / 全文翻译 / 选中翻译历史 / 解析结果 / 历史`. Analysis results retain Summary / Notes / References as inner tabs. Keep keyboard tab semantics and scroll state; do not duplicate Zotero tags.
- Reader `解析` opens the current PDF detail with progress and Stop. Repeated clicks focus the same active task. It never creates, selects or appends to Chat, a Webapp session or a remote file record. The main Stop cancels preparation/generation and pauses the associated reference task; retained text remains readable.
- History groups by the verified Zotero parent item and library; standalone PDFs stay separate. Every result retains its exact attachment identity. Include extraction-only and selection-only papers. The history tab lists all retained versions across attachments and links to the exact result. Opening a PDF revalidates its saved identity; reading stored results never starts work.
- Make the paper title the detail's visual center. Summary/notes use a restrained 78ch reading column, fine dividers and compact tools instead of nested cards. Tabs remain reachable while scrolling. Each tab keeps its scroll, reference search/filter and selection. Summary uses safe Markdown; notes use existing category/quote/explanation parsing with readable fallback and copying. Copy includes warnings; model page labels do not become PDF navigation controls. Reference rows separate numbering, original citation, verification status and actions. Search/status controls sit above the list; selection count, writable destination library/collection and import are grouped below. Unverified rows retain source location, explicit publication search and available source links. DOI or normalized-title matches enable import; author/year differences and AI readiness never gate these actions. No task means no pause/verify; no importable selection means no batch import. Loading, paused, failure, incomplete-save and empty results are explicit, with English/Chinese and 12–24px light/dark layouts.
- Back up readable notes before native writes, then update the same history entry with the write outcome. A malformed annotation must not discard other valid notes or successful writes. Local format repair makes no additional AI request. Incomplete/failed/cancelled generation retains already received text with an explicit warning and writes no native annotations; failures before text retain their original error. Do not automatically retry an uncertain native save.
- Recovered formatting and retained partial-generation notices use neutral status styling. Actual history-persistence or native-write failures use error styling; a readable recovery must not look like complete data loss.
- History stores at most 200 records and 1,000,000 serialized characters, evicting the oldest records; long notes can leave substantially fewer than 200. If persistence or the final write-status update fails, keep at most 10 latest results in the current Manager window with a visible copy-before-close warning. Closing or restarting loses these unsaved results; the UI must not describe them as durably saved.
- `解析配置` chooses an accessible Jadense route/model or a saved BYOK model for analysis only. Provider/key/model catalog editing remains in Settings → BYOK. Missing feature preferences are snapshotted from legacy settings once before any catalog mutation, preserving the existing analysis selection.
- If a specifically saved BYOK model was deleted, belongs to a removed Provider, or is otherwise no longer usable, keep the unavailable selection visible and fail the requested analysis before dispatch. Ask the user to reselect; never fall back to another feature’s choice, another BYOK model, or Jadense. This error must not block ordinary Chat, translation, upload, or existing history.

### Jadense Academic Section

The visible `攻玉学术` section retains the internal compatibility value `migrate`. It is the Manager surface for the Jadense account and one-way upload, grouped into two top bookmark tabs—`用户信息 / 文献同步`—with the same `tablist`/`tab`/`tabpanel` semantics, keyboard navigation, and underline indicator as the literature-analysis tabs. Token configuration lives in the Settings `连接攻玉` tab.

- `用户信息`: the `你的攻玉 / Your Jadense` card shows account display name, subscription, effective points balance and source (`个人积分` or `团队积分`), daily check-in state, current streak, and today's granted points. `前往攻玉`, `打开签到页`, and `订阅与用量` open the corresponding pages in the system browser. Check-in information is read-only; checking in happens on the Jadense website. Primary and fallback balances are transport context and must not be added together.
- `文献同步`: favorite-folder selector, refresh action, and PDF-upload default; current Zotero collection/item summary; actions for uploading selected items or the selected collection; and a progress/result log with separate metadata/PDF outcomes. Do not duplicate the folder/PDF controls elsewhere in Manager.

The Settings `连接攻玉` tab contains the masked Jadense token with Copy/Edit actions, token help, the `生成 / 更新令牌` Webapp action, and Disconnect. The canonical Jadense server URL remains fixed in the UI; existing developer/staging preferences remain readable. Copy stays user-facing: say `这台电脑` rather than `Zotero profile`, never expose OAuth scope names, and point recovery guidance at the concrete tab (token problems to `设置 → 连接攻玉`, points/check-in to `攻玉学术 → 用户信息`).

Connection presentation has four stable states: `未连接`, `正在连接`, `已连接`, and `连接失败`. Saving a token starts verification without moving the page. A missing token shows the connection form and keeps the current Zotero selection visible. Reopening the account page or choosing Refresh rereads the authoritative effective balance and check-in status; the plugin does not submit check-ins or automatically retry failed AI requests.

Profile and points/check-in status are independent optional projections. Their loading, network, server, or malformed-response failures stay inside the affected block, preserve other usable connection/upload state, and offer a local retry. A bearer `401` prompts token replacement without silently deleting the stored token. A `403 insufficient_scope` disables only the account feature that needs the missing scope; Chat, favorites, and uploads retain their existing permissions.

The existing server token scopes remain compatible: `points:read` permits the balance/check-in snapshot and `points:check-in` remains in the server contract for older clients. The read-only account view does not revoke tokens, widen existing tokens, or change server check-in behavior. The current plugin has no direct check-in action.

The three Webapp actions in `用户信息` are `前往攻玉` (`/`), `打开签到页` (`/app/check-in`), and `订阅与用量` (`/app?settings=billing`). The Settings `连接攻玉` tab separately provides `生成 / 更新令牌` (`/app?settings=integrations`). Build them from the active normalized Webapp origin and open them through Zotero's system-browser entry. Never navigate the privileged Manager to Webapp content or put the extension token in a URL.

When the Jadense AI route returns `402 POINTS_INSUFFICIENT`, keep the failed message local and direct users to the website for check-in or additional points. Manager can guide them to `攻玉学术 → 用户信息 → 打开签到页`; Reader-local guidance may point directly to the Jadense academic homepage. Neither path automatically retries the request.

Disable only the upload action that is missing a usable token, target folder, or relevant selection. A missing folder or an optional account/status failure must not disable Chat or reader tools. Upload always uses the Jadense connection regardless of the active AI route.

### Settings Section

Manager Settings uses accessible `常规 / 功能配置 / 快捷键设置 / 连接攻玉 / BYOK` tabs in that order, opening `常规` by default. General owns `显示语言` (`跟随 Zotero / 简体中文 / English`), `主题设置` (`跟随 Zotero / 浅色 / 深色`), interface font size, translation window style, and background transparency. Keep all controls in the same settings card, aligned as labeled rows with help text; use a grouped font stepper/reset, the existing style select, and a 0–100% transparency slider with its current value. Language changes save for the next Zotero restart, with an explicit restart hint; theme changes apply immediately across open plugin surfaces. The feature tab lists `AI 对话`, then `自动跟随当前对话模型`, followed by `实时翻译 / 文献解析 / 图片解读`. The toggle defaults on, keeps the AI Chat selector editable, and disables the other three searchable Jadense/BYOK selectors; turning it off enables independent editing while preserving saved choices. Use the same compact, searchable, icon-bearing model selector as the Chat composer. Shortcut recording/save/disable/reset, Jadense token configuration, and BYOK editing remain in their own tabs. Account, folder, PDF, and upload controls belong to `攻玉学术`.

The composer always exposes both model sources and is the authoritative current Chat model when automatic following is enabled. With following disabled, it edits the Chat selection for text and uploaded images, and the image-interpretation selection when the latest image came from the Reader, so subsequent image questions remain consistent. `文献解析 → 解析配置` edits the same analysis choice as feature settings and is disabled while following is enabled. Empty/offline model catalogs cannot prevent selecting configured BYOK models. Keep saved unavailable choices visible, with a local actionable error for that feature. BYOK surfaces use the community-standard field terms `Provider / Model id / API base url / API key`; preserve actual protocol and model proper names.

`BYOK 配置` separates Provider connection records from their model catalogs. The Provider list is an embedded, keyboard-accessible sidebar: users can browse, switch, add, edit, and delete providers without opening a dropdown. A Provider owns display name, protocol, an initially empty API Base URL, and one password-style API key field with a small show/hide-eye control. Each model owns a local display name, exact Provider Model ID, optional context window, and output cap. The model catalog stays visible as a compact list in the normal state; adding inserts an unsaved single-column form below the catalog header and before the list, while editing replaces the selected row in place. Existing saved URLs and keys remain readable, while new providers never imply OpenAI by pre-populating an address.

Native Zotero Preferences remains the fallback configuration surface when Manager cannot open. It starts with `常规`, then the AI Chat selector, automatic-follow toggle, and the remaining feature selectors, followed by `连接攻玉` and `BYOK 配置`, backed by the same local preferences. Account, points/check-in status, and upload remain Manager-only.

Saving an empty replacement keeps the corresponding Provider key. Deleting a Provider also deletes only its models. Changing a token, the automatic-follow toggle, a running feature’s model selection, or saved BYOK configuration cancels affected pending reading/AI actions so material from an earlier operation cannot be sent to a new destination. Clearing BYOK does not clear Jadense or change feature selections. Saving is offline; Test uses the unsaved active Provider/model form for one real, potentially billable request capped at 3,000 output tokens and never changes history or saved configuration. A valid streamed response that ends at this probe cap proves connectivity. BYOK Chat still reports truncated generation as a failure; paper analysis retains received text for reading while withholding native annotation writes.

The compact composer selector identifies the effective Chat/image choice: a Jadense route/model or a local BYOK model, without expanding the footer height unnecessarily. The sidebar connection dot continues to report only the Jadense connection; it must not imply BYOK readiness or mirror an optional account/status-card failure.

## Native Item-Pane Panel

The panel is intentionally compact. It appears in Zotero's item pane, so it should not be treated as the primary collection migration UI.

### Layout

- Use a single-column panel with 12px chrome text and 30px to 32px controls.
- Keep buttons full-width for easy scanning.
- The first action should open the Jadense Manager.
- Show current connection, default folder, selected item count, and current collection status as short status rows.

### Actions

The panel can expose:

- Open Jadense Manager.
- Export selected items.
- Export current collection when a collection is selected.

Panel copy should make it clear that upload lives in the Manager `攻玉学术` section and collection context menu.

## Menu And Entry Rules

- Tools menu must be a visible `Jadense` submenu.
- The main Zotero window must load the plugin Fluent resource before menu registration.
- Menu entries should never depend on webapp navigation.
- Configure opens the Manager Settings `连接攻玉` tab first; Zotero Preferences remains the fallback path.
- Collection context menu opens Manager `攻玉学术` and may also expose direct collection export.

## Native Reader Tools

- The permanent toolbar logo is a native button named `打开攻玉工作台`, with hover and keyboard-focus feedback. Clicking it opens the Manager Chat section or focuses the existing window, retaining the active conversation and draft without attaching the current paper or dispatching AI.
- The permanent toolbar has no language selectors, language menu, translation-options menu, or separate References action. Existing per-article language preferences remain readable; sentence language overrides live in the translation panel.
- The translation panel header exposes `本句源语言 / 本句目标语言` and `重新翻译`, with a short explanation that changes apply only to the current sentence. Changing a selector clears the old result and invalidates old deltas; sending is explicit and retains the panel's original source/page even if PDF selection changes. Disable duplicate sends while generating; allow language changes, retry after errors, and Escape dismissal. New selected text always starts from the article's preferences. Translation history displays the actual source/target language of each completed result.
- Use Zotero's toolbar and text-selection popup extension slots, with compact labels and accessible button names. Do not restructure Zotero's own controls.
- Main toolbar offers `提问 / 解析 / 引用 / 全文翻译`; `提问` uses a chat icon and tooltip `发起新对话，向 AI 提问（当前文献）` and opens a new local Chat draft for the active document; the first question creates the session. `解析` opens `文献解析 → 解析历史` and starts an independent analysis for that exact attachment without writing Chat. The same entry also extracts references for the same PDF; show them in `文献解析 → 参考文献`, and contain extraction errors there without blocking summary/notes. Selection translation is absent from both the main toolbar and its collapsed menu; its shortcut remains available. Selection popup offers `智能翻译 / 引用选文`; read the current selection when invoked and explain when no text is selected.
- Figure interpretation is a separate canvas-adjacent layer, not another item in either toolbar. On Zotero 10.0.1+ ordinary PDF views, pointer hover over a valid SDT image shows one subtle 2px brand-green outline that cannot intercept mouse input. Click locks it and reveals two vertically grouped buttons: `图片解读（开启新对话）` and `图片解读（追加在当前对话）`. Clamp the group to an 8px viewport inset, allow long labels to wrap in narrow/split views, and explicitly cycle Tab/Shift+Tab between the actions because PDFView owns the native Tab sequence. Do not show them before selection. Escape, blank click, or page change clears them, and zoom, rotation, scrolling, redraw, and split-view changes keep the geometry aligned.
- Install independent controllers for primary and split PDF views. Selection is available only for the ordinary pointer tool when the user is not dragging; it must not hinder text selection, annotations, or native controls. While either branch is cropping and handing the action to Manager, disable both buttons and show one `处理中…` state on the activated button instead of accepting a duplicate request. Remove all figure nodes and listeners when the view or plugin closes.
- Attach a caption only when same-page SDT text begins with `Figure`, `Fig.`, `图`, `Scheme`, or `Plate`, excludes `Table`/`表`, aligns or overlaps horizontally, and lies within `max(24pt, 6% page height)`. Merge adjacent caption parts, prefer below-image then nearest geometry/document order, and omit an equal-ranked result. No-caption is a normal actionable state.
- Crop only after either image-interpretation button is activated. Preserve aspect ratio, limit the longest edge to 2048px and decoded size to 6 MiB, prefer PNG, then progressively use JPEG quality/dimension reduction. If SDT is unavailable, omit automatic recognition while retaining manual capture when native region cropping is available; if a selected crop still cannot meet the limit, show a Reader-local error and make no AI request. Manual selection is an independent shortcut fallback: drag within a PDF page, retain the green frame and both interpretation actions plus `退出`; Escape or Exit clears the selection without dispatch. Use native region crops and PDF coordinates so zoom/rotation remain aligned, even without SDT. Do not add OCR, EPUB/web-attachment support, or change the existing toolbar.
- Translation opens immediately as a movable, resizable reader-side floating panel with original text, streaming result, Copy, Close, Escape dismissal, and inline error state. Before dispatch, matching active-selection character geometry restores supported Unicode super/subscripts that Zotero's flat popup text omitted; missing or ambiguous geometry leaves the source untouched, and complex/scanned equations are not guessed as text. Its shared model prompt treats recovered scripts as format evidence and requires directly renderable Markdown, `$...$` inline formulas, three-line `$$...$$` display formulas, unchanged LaTeX semantics, and no formula code fences. The result uses the same safe Markdown presentation as Chat in both the reader panel and Translation History, typesetting common inline/display LaTeX formulas through bundled KaTeX MathML while preserving genuine fenced code blocks; archive and Copy retain the Markdown source. It is a non-modal reading aid, not a Chat surface, and a second translation replaces the visible panel content without mixing either result into conversation state.
- Group reader actions with the existing 20px Jadense logo, 16px action line icons, 28px controls, and plugin-scoped theme variables controlled by General settings. Use the original logo geometry/gradients as inline SVG with an accessible brand name. At reader widths up to 1400px, or when the native toolbar has insufficient space, collapse the entire action area to one `阅读操作` toggle beside the logo. Its action list opens over the document without widening the toolbar; support arrows/Home/End, Escape, outside-click dismissal and focus restoration. Preserve Zotero page controls and selection-popup labels.
- Selection-popup actions fit the native content width. Keep both full labels and accessible hit targets; wrap actions onto separate rows when font size or translated labels need more room. Do not widen, clip, or restyle Zotero's color and annotation controls. Verify plugin font sizes 12–24px, both UI languages, and light/dark themes against the actual native popup.
- Missing selection shows a local status hint that dismisses with Escape or after five seconds; do not open Manager or dispatch an action. The reader's XHTML/content boundary may not load plugin `chrome://` images, so the logo must be inline and require no external resource request.
- Annotations are native highlights with original sentences, Chinese explanatory comments, category colors, and tags. Use Zotero's standard annotation colors for research categories, not the brand palette.
- Categories are 研究问题、核心论点、创新点、研究方法、关键证据、研究结论、局限性、未来工作; unknown additive categories fall back to 补充要点.
- Never fabricate a highlight when a sentence lacks verified local coordinates. Preserve human annotations and original PDF bytes; users can export native annotations with Zotero's PDF export.

## Visual Rules

- Primary green: `#16cf8c`.
- Main content background: `#ffffff`.
- Zotero plugin sidebar/chrome background: `#f7f8f5`.
- Text: `#111510` or close near-black.
- Muted text: `#53625a`.
- Borders: 1px, low opacity or `#d9e2dd`.
- Maximum ordinary radius: 8px. Compact buttons and inputs should use 5px to 6px.
- Avoid large shadows, decorative gradients, and large rounded promotional surfaces.
- Default ordinary action/navigation buttons to ghost (transparent background and border), with hover and keyboard-focus feedback. Retain emphasized primary actions, destructive text, and input/select field borders; do not style every action as an outlined button.

## Validation Checklist

- Tools menu labels render as visible text.
- Main command opens Chat; Configure opens Settings → `连接攻玉`, while collection actions open `攻玉学术` and retain the internal `migrate` section value.
- Navigation exposes New chat / Literature analysis / Jadense Academic; Help and Settings retain their existing placements. Legacy translation navigation routes to Literature analysis. Verify the five result tabs, two top history/configuration tabs and inner analysis tabs.
- Manager controls stay readable at 760px by 620px and larger.
- Missing token, missing default folder, and missing selected collection each produce distinct disabled states.
- Disconnected, loading, connected, invalid-token, missing-scope, and optional account/status failures remain distinguishable without shifting the layout or disabling unrelated capabilities.
- Settings contains `常规 / 功能配置 / 快捷键设置 / 连接攻玉 / BYOK`, with General first/default, an enabled-by-default automatic-follow toggle, four model selectors matching the Chat selector, editable screenshot/translation chords, and a gear entry. Native Preferences starts with General, then feature, connection, and BYOK sections.
- Verify Chinese/English labels, tooltips, accessible names, and dynamic states after restart; changing UI language must preserve source text, AI output, translation direction, and existing history. Verify light/dark/following across open plugin surfaces and cleanup on unload, without recoloring Zotero/PDF roots.
- `你的攻玉` retains account, balance, and read-only check-in status; `打开签到页` launches the website without a token in the URL. No plugin UI interaction submits a direct check-in request.
- Reader analysis adds one post-0.3.1 record with a default summary and expandable/copyable notes to the independent history and no Chat session/message. Verify local format repair, partial-stream retention with zero native writes, per-annotation failure isolation, final write status, old summary-only records, and the unsaved-result copy warning. A stale saved analysis BYOK selection fails before dispatch without changing other model selections or unrelated features.
- A translation-history title opens the revalidated local attachment in Zotero Reader at its saved physical page; stale identity failure stays local to that link.
- Figure hover, lock, both keyboard-focusable conversation buttons, Escape/blank/page cleanup, pointer/drag gating, primary/split geometry, and shared busy-state duplicate protection work in the installed Zotero 10.0.1 XPI without adding nodes to the permanent or selection toolbar.
- New-conversation figure interpretation sends exactly one bounded image with the correct page and unique caption, automatically associates the parent literature and current PDF's bounded extractable text, and streams into one new dedicated session. Current-conversation interpretation sends into the active session without creating or resetting it. Both show each sent image in its own user message and reload local attachments after reopening. Follow-ups attach only the latest image. Preferences persist references, never image bytes or hidden prompts. Missing/ambiguous captions, unavailable text/SDT/crop capability, oversize crops, and Provider vision errors remain local and do not alter other model selections or unrelated reader tools.
- Panel remains usable when only items are selected and does not promise collection-level visibility.
- XPI includes manager XHTML, CSS, script, locale files, and icons.
- Sources, empty/unreadable states, streaming/cancel/error states, dark mode, and the native selection actions remain usable in the compact layout.
- The Jadense composer selector loads with a `chat:temporary` token, supports grouped search and subscription-marked locked rows, persists route/direct-model choices, defaults to an explicit `deepseek-v4-flash-vision-exp` model ID, sends mutually exclusive fields, and keeps the saved/default model usable if the optional catalog is unavailable.
- Native toolbar/popup screenshots must come from the installed XPI in an isolated Zotero profile; a browser fixture proves only Manager layout/interaction. Verify actual viewport sizes before claiming wide/compact coverage.

### Translation Window Appearance And Geometry

Manager translation history uses full-width, left-aligned rows: a quiet type badge, paper title, secondary language/page/status/time metadata, and a one-line selection preview. Expanding a selection shows labeled source/translation columns that stack at narrow widths. Local search matches title, saved citation/item key and selected-text source/result; whitespace-separated keywords intersect, ignoring case and width variants. Type and rolling 7/30/90-day filters combine with search, show matching/total counts, and offer Clear filters. Search controls retain focus during input; filters survive refresh and returning from full-document details. Empty history and no matches have distinct messages. Search never reads PDF files or dispatches AI requests.

Full-document translation lives in the existing Jadense native item-pane section. The library retains its sync panel; PDF tabs use a reading workspace tied to that Reader's attachment. At narrow widths Zotero can switch the native pane to a bottom stack; temporarily dock the same plugin root beside the PDF without moving its browser or rebuilding prose. Independent Reader windows use this dock too. Restore the native width/pin state when leaving; remember the reading width (first target 480px, constrained by the host). Do not show unavailable Chat/Analysis workspace tabs.

The workspace has a compact header, `阅读 / 定位`, reliable chapter outline, `Aa`, `⋯`, one scrolling article, and a small status footer. Default prose is 14px/1.8 with 0.8em paragraph spacing, 16px medium-weight headings, 24px horizontal padding (16px in narrow panes); tools stay 12–13px. Body font (12–24px) and line height have independent preferences. At 600px workspace height the body takes at least 80%. Use opaque light/dark reading backgrounds, left-aligned text, and local horizontal overflow for math/tables/code. Never insert page headings, source previews, extraction diagnostics, or per-paragraph action buttons into prose. Reuse the host paper title and suppress exact duplicate title blocks even after a copyright notice.

Read mode supports ordinary text selection. Locate mode changes only interaction and a quiet hover/focus marker: pointer clicks and Enter/Space locate the actual source block, with Arrow/Home/End keyboard movement. Selection drags, selected text, links and modified selection shortcuts do not locate. Keep page ranges and alternative locations in the footer. Use a transient native PDF highlight, or physical-page navigation when rectangles are absent. Cross-page paragraphs start at the first source position. Never create annotations or synchronize scrolling. Mode switches retain DOM/selection; updates replace only changed paragraphs. Save the block ID plus fractional in-block offset per translation record, and preserve it through font/width changes and reopening.

Full translation renders readable Markdown increments while generating; label unfinished text as a draft, and count only committed capacity chunks in progress. Show OCR physical page progress, waiting for the first response, and the active chunk separately. During generation force Read mode and disable Locate across buttons, pointer and keyboard routes. After stopping, completed chunks can locate their covered OCR source regions. Footer locations use physical `pageIndex + 1 / totalPages`, never journal page labels. Adjacent unfinished passages form one quiet notice; failures retain drafts and completed text. Put Copy all, history and explicit retranslation in `⋯`; progress/pause remain in the footer. Closing the view does not pause DocumentJobs. Manager history uses the same reader; old extraction records are read-only and opening them never starts OCR or translation.

Feature settings in Manager and native Preferences offer Install and start local OCR, installation status, and an expandable context/output capacity override used when model metadata is absent. Keep these controls readable in narrow panes and both themes. The installer uses a private local Python environment; failures offer retry and the manual README steps. Formulas remain local source crops, rendered at their preserved placeholders; neither formula recovery nor individual formula requests are required for full translation.

Selected-text translation windows retain a bottom-left `外观` disclosure for style and background transparency. Preferences update every open window immediately. Transparency affects the background only; text and buttons remain opaque. Glass must visibly blur the underlying PDF. On Gecko, use a live native element image of each PDF reading pane in the window's own blurred background layer, with an adjustable theme tint; plain backdrop CSS alone did not produce the required native effect. Inner reading surfaces stay transparent, and high-contrast mode restores an opaque system background. Drag the header to move, or any edge/corner to resize within the reader viewport. The focusable header supports arrow-key movement and Shift+arrow resizing; reading, selection, form controls and copy must remain usable. At the 300×220 minimum size and 24px font, the body and appearance menu scroll within their available height while the bottom toggle remains reachable.

## Unified PDF Reader Sidebar

- 工作台和原生功能配置均提供「翻译方式：AI 生成 / 翻译接口」。默认 AI 生成，沿用既有模型；翻译接口显示独立 Bing / Google 服务选择，隐藏 AI 翻译模型行，不受自动跟随开关影响。两类配置切换保留已有值，不改变 AI 对话、解析、图片解读。传统模式只输出译文，选文和全文沿用既有阅读与历史界面。
- 「翻译方式」与「翻译服务」标签旁使用可聚焦说明图标。悬浮/聚焦展示当前类别的能力边界和原文直传所选服务的说明，Escape 关闭提示；说明不常驻占据设置页。提示继承插件浅深主题，窄窗口保留完整标签与可用控件宽度；中英文文案保持相同含义。

- The native Jadense pane and standalone dock share five pages: Chat, Full Markdown, Full translation, Selection translations, Analysis results. The last four bind to the current PDF, while Chat keeps all conversations reachable. Each result offers a workbench deep link. Preserve drafts and reading positions while switching.
- Reader Chat uses the shared composer with plugin-owned textarea colors, placeholder and focus decoration in both themes, overriding native field styling only inside the plugin. Manager and Reader share the same compact inset status panel tucked behind the composer with no vertical gap. Reader adds PDF association to that panel; Manager hides the whole panel when status is empty. The dock and the area around Back to latest are transparent, without a backdrop mask; measured dock height reserves message scroll space and positions the floating latest button. Empty status occupies no height; errors remain readable and may wrap.
- The shell fills its actual host width and available height. Default width is 480px, the minimum usable target is 320 × 360px, and a smaller host is contained instead of overflowing. Never set a fixed content-height cap based on the previous scroll content. Size tests must resize the host, not constrain the plugin root and leave unused sidebar space.
- New Reader conversations associate the Reader's PDF. Selecting an existing conversation alone changes no sources. When the PDF is missing from that conversation, the composer states “发送时将关联当前 PDF”; Send (including its keyboard shortcut) confirms association before dispatch. Text extraction warnings remain visible; an invalid attachment cannot be replaced with the globally selected library item.
- Messages, Markdown/image actions and composer are shared with Manager. Each view keeps its selection and drafts independently; switching pages retains scroll and drafts. The plugin owns the single Chat generation, so hiding or closing a view does not stop it. The Stop tooltip names the generating conversation.
- Translation reuses continuous reading, source positioning, appearance, progress, pause/resume and history. Opening its page restores results without starting work. Custom controls and bounded portal menus inherit plugin colors only; they never recolor Zotero's PDF or chrome.

- 模型触发器复用 webapp/public/model-logos/catalog.json 索引的品牌 Logo，按实际模型 ID 匹配（含 BYOK），智能路由保留模式图标；未知品牌保留后备标识。彩色素材保持原色，单色素材用主题前景色 mask；构建直接打包公共素材。触发器常规宽度显示图标与名称，Reader 对话内容容器不超过 380 px 时仅显示图标（32 px 按钮），完整名称保留在悬停提示和菜单中。侧栏高度按宿主可见区域计算，不能用自身内容高度反向限制；原生 context-pane 的最小宽度须容纳右侧宿主图标栏。

- Reader selection history shows only the current attachment’s exact item/library/key identity; the workbench aggregates all attachment histories under their parent paper. Local source navigation remains separate from Markdown links.

### Two-step source and translation

Original extraction is a model-independent action. Full translation is available without a pre-existing source: clicking Translate full text silently extracts missing Markdown and continues, or directly reuses an existing source, without a confirmation or page switch. Browsing never starts work; independent extraction completion never dispatches translation. Source and translated versions remain separately selectable. Persist image assets separately and resolve only registered local resources. Missing images display a local notice without hiding readable text. Re-extraction creates a new source version; existing translations retain their original source. Keep narrow sidebars readable with compact selectors and horizontally scrollable, unbroken tab labels.

### 文献成果组件与运行状态

新增文献列表、附件详情和 Reader 成果页统一使用 `content/ui.css` 的主题控件与布局。搜索框显式移除系统外观；版本、附件及语言复用自制 `createJdxSelect`；选中翻译用按钮控制的折叠内容，保留 `aria-expanded` 与关联面板；译文工具菜单用自制面板，Escape 关闭并恢复触发器焦点。保留 HTML 按钮和输入的键盘语义，不依赖系统 select、details 或 progress 的视觉呈现。

文献行采用题名、书目信息、成果类型与活动时间的阅读层级；详情使用紧凑导航与成果页签，窄窗口页签自动换行以保持所有入口可见。空状态提供短标题与下一步说明。主题色仅作用于插件根节点，沿用绿色状态信号、绿灰导航、细分隔线和轻量圆角。

提取状态置于操作区下方，译文状态置于阅读器底部；进行中、暂停、部分完成、错误与保存风险均保留可读文案。自制进度条提供 progressbar ARIA 值，暂停后保留进度；完成后不显示过期阶段文案。未完整保存仅在译文底部提示一次，并直接提供「复制全文」，不要求用户寻找工具菜单。错误文字允许换行，正文与暂停/继续操作保持可达。

文献详情适配外层 panel：占满可用宽度，外层和正文仅保留 12px 内边距，不使用居中的 960px 限宽。操作栏透明且无可见控件时隐藏。工作台将解析总结、解析笔记、参考文献展开为一级成果 tabs；Reader 也将三个解析页平铺在左上角页面切换下拉菜单中。已保存的提取完成状态不常驻；本次提取、翻译完成与复制反馈使用 3.5 秒浮动 toast，不占正文布局。进行中与暂停、失败、保存风险仍可读取。

成果阅读页及详情导航的操作按钮统一使用 18px 线性图标、32px 点击区域、原生 title tooltip 与 aria-label，动态暂停/继续、重新提取、导入数量须同步可访问名称。tabs、菜单选项及当前选择值保留文字。全文翻译为胶囊组合控件：左侧目标语言，右侧「全文翻译」文字按钮，内部无间距；窄侧栏允许整个胶囊换行，不能拆开语言和按钮。

所有成果操作栏的图标从左侧起连续排列，不给工作台按钮设置自动起始边距，不将单个复制按钮右对齐。版本选择排在图标之后，翻译胶囊可保留右侧位置。Reader 页面菜单为对话、全文 Markdown、全文翻译、选中翻译历史、解析总结、解析笔记、参考文献；解析页面不再嵌套 tabs，工作台跳转保留当前解析页面。
