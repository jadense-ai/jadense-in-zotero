# 安全说明

## 开源边界

本仓库公开 Zotero 客户端及其构建、测试资源，包含攻玉 HTTP 接口调用和 BYOK 适配器。接口路径、插件 ID 和客户端提示词不是服务端授权依据。真实用户身份、权限、计费及 Provider 服务凭据由攻玉服务端或所选 Provider 管理，不包含在本仓库中。

首版审计未发现候选源码中的硬编码服务秘密或主应用服务端依赖。该结论限于已检查的源码和构建范围，不是对在线服务的全面安全认证。

## 用户配置

攻玉令牌和 BYOK Key 由用户输入并保存在本机 Zotero profile；不能假设该 profile 是加密凭据库。不要上传 profile、prefs.js、环境文件、请求原文、访问令牌或包含个人数据的截图。出现泄露时，先在签发服务撤销或轮换相应令牌。

BYOK 的 Base URL 决定请求和凭据发往何处，请使用可信的服务端地址。使用纯 BYOK 不需要攻玉令牌；若已经配置攻玉连接，账号卡片和自动更新等独立功能仍可连接攻玉。

## 报告漏洞

公开 GitHub 仓库启用 Private vulnerability reporting 后，请通过 Security → Report a vulnerability 私下提交可复现问题。若该入口尚未启用，请先通过攻玉官网提供的支持入口联系维护者，取得私下报告渠道；不要在公开 Issue 中提交可利用细节、令牌或用户文件。

报告可包含受影响版本、操作系统、Zotero 版本、使用合成数据的复现步骤和影响范围。复现应使用临时 profile 和模拟凭据。普通使用问题可通过公开 Issue 反馈，但同样需要去除个人内容。
